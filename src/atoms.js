import { atom } from 'jotai';

export const isSceneLoadedAtom = atom(false);
export const isSoundEnabledAtom = atom(false);
export const isBigNoiseActiveAtom = atom(false);
export const isGlitchActiveAtom = atom(false);
